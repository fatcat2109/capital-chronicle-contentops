# cc-live-contentops

Future live control plane skeleton for Capital Chronicle.

**CURRENT STATUS:**
- This repo is a future live control plane skeleton only.
- No keys, no network, no provider calls, no platform APIs, no scheduling execution, no publishing, no autonomous replies/DMs.
- No core repo mutation.
- It will receive structured artifacts from cc-contentops later.
- All live capabilities require future explicit GO.

**COMMANDS:**
- `python -m live_contentops.cli status` : Show local skeleton status.
