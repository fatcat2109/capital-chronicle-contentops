# Telegram credentials placeholder template (EXAMPLE ONLY).
# Copy this to a LOCAL env file out-of-band. Never commit real values.
# Never paste real token/chat ID into ChatGPT, Cline, the repo, or evidence.
# Rotate the token out-of-band if it was ever printed, logged, or exposed.
TELEGRAM_BOT_TOKEN=REDACTED_NEVER_COMMIT
TELEGRAM_TARGET_CHAT_ID=REDACTED_NEVER_COMMIT
