# Static Prototype

This directory contains pure, offline, static HTML/CSS files for inspecting the operator UI.
There is NO npm, NO node, NO react, and NO server required. Simply open `index.html` in a local browser to view the mocked layout.

It strictly adheres to all safety boundaries established in 0127-0135.
