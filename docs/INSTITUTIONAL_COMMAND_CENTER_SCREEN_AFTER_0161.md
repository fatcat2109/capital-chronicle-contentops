# Institutional Command Center Screen (After 0161)

Task label: TASK_CONTENTOPS_0161_INSTITUTIONAL_COMMAND_CENTER_SCREEN_V0
Repo: A:\Capital Chronicle\tools\cc-live-contentops
Branch: master
Baseline HEAD before this task: 1c03ca0 — "feat: add institutional shell prototype"

This task upgrades the Command Center screen inside the static institutional shell
prototype (`ui/institutional_shell/`) into the first institutional-grade
operational screen. It remains static, local-only, fixture-driven, with no
backend, no dependency, no network, no env reads, and no live controls.

## 1. Owner Decision

The owner has decided to make the global safety and readiness state legible in
under 10 seconds via a rich Command Center: hero status band, safety ribbon,
executive status cards, gate timeline, blocked action matrix, evidence summary,
and read-only state panels for Telegram, publish automation, content studio, UI
rebuild, and residual drift.

## 2. What Changed (within ui/institutional_shell only)

- `fixture_data.js`: added `command_center_detail` with hero band, 8 executive
  cards, 12 gate-timeline items (0157–0168), 13-row blocked action matrix,
  evidence summary, telegram gate state, publish automation state, content studio
  state, UI rebuild state, residual drift panel, next allowed action panel. Also
  added the `MANUAL_REVIEW_REQUIRED` global safety banner.
- `app.js`: added a dedicated `renderCommandCenter` path that renders the hero
  band, ribbon, executive cards, gate timeline, blocked action matrix (disabled,
  read-only), and the read-only state panels. Also fixed a latent missing brace
  in `renderNav` from 0160 so the file parses cleanly.
- `styles.css`: added Command Center hero/section styling (institutional, dense,
  no market-direction color semantics).
- `README.md`: documented the Command Center.

## 3. Command Center Zones

1. Hero status band: title, system mode, accepted HEAD, kill switch, public
   state, live/API state, current gate, next allowed action.
2. Safety ribbon: LOCAL_ONLY, DRY_RUN_ONLY, REVIEW_ONLY, MANUAL_REVIEW_REQUIRED,
   NOT_PUBLIC_POSTABLE, LIVE_DISABLED, KILL_SWITCH_ACTIVE, SECRET_REDACTED,
   NO_FINANCIAL_ADVICE, NO_SIGNAL_LANGUAGE.
3. Executive status cards (8): System Safety, Build Baseline, Publish Automation,
   Telegram Pilot Gate, Evidence / Audit, UI Rebuild Track, Content Studio Track,
   Residual Drift.
4. Gate timeline: 0157–0161 accepted/current plus future 0162–0168.
5. Blocked action matrix (disabled, read-only): live posting, scheduler, platform
   API, provider/LLM API, scraping, autonomous replies/DMs, one-button
   publish-all, public-ready final copy, credential display, raw env paths, raw
   request URLs, raw platform responses, broker/execution/order routing.
6. Evidence summary: full suite, focused tests, CLI summaries, secret scan,
   forbidden scope, git status, residual drift, evidence packet requirement.
7. Telegram gate state (read-only): credential presence redacted, official docs
   gate implemented, live getMe not run, channel write permission unvalidated,
   sendMessage disabled, live adapter disabled, posting disabled.
8. Publish automation state: dry-run only, manual approval required, live
   disabled, one-button publish-all disabled.
9. Content studio state: review-only, source/evidence required, not
   public-postable, no final social copy generated by repo.
10. UI rebuild state: 0157–0160 accepted, 0161 current, Antigravity future-only,
    no browser QA yet, no screenshots yet.
11. Residual drift panel: local env file and strategy docs untouched/untracked;
    no cleanup commands allowed.
12. Next allowed action panel: AWAIT OPERATOR/CHATGPT AUDIT_OF_0161_EVIDENCE
    BEFORE_ANY_NEXT_TASK; future task 0162 only after audit.

## 4. Safety Posture (Enforced)

- Static/local-only, fixture/mock-data-only.
- No backend, no dependency, no `fetch`/XHR/WebSocket/EventSource, no remote URL.
- No live posting, scheduling, scraping, autonomous replies/DMs.
- All forbidden/blocked actions render only as disabled, read-only text.
- No secrets, env paths, request URLs, raw platform responses, or raw vendor data.
- No financial advice, no signal/trading language, no buy/sell/long/short cues,
  no bullish/bearish (red/green = market-direction) semantics. Red/green are
  operational PASS/BLOCKED only.

## 5. Validation Surface

- Schema: `schemas/institutional_command_center_screen_packet.schema.json`.
- Validator + summary: `live_contentops/institutional_command_center_screen.py`.
- CLI summary: `python -m live_contentops.cli pre-alpha-institutional-command-center-screen-summary`.
- Tests: `tests/test_institutional_command_center_screen.py` (static-asset
  inspection, no browser).

## 6. Relationship To Telegram Live-Gate Sequencing

This screen does NOT supersede Telegram live-gate sequencing. The Telegram gate
state panel is a read-only, redacted display only; it never calls getMe or
sendMessage and never reveals credentials.
