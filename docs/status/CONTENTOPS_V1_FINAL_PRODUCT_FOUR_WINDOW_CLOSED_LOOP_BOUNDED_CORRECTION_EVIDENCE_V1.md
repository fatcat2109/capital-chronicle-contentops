# ContentOps V1 Four-Window Closed-Loop Bounded Correction Evidence V1

Authority date: 2026-08-16

Task: `TASK_CONTENTOPS_V1_FINAL_PRODUCT_FOUR_WINDOW_CLOSED_LOOP_BOUNDED_CORRECTION_V1`

Result:

`PASS_V1_FOUR_WINDOW_CLOSED_LOOP_BOUNDED_CORRECTION_READY_FOR_SINGLE_LIVE_CANARY`

This is a bounded correction result. It is not `V1_FINAL_PRODUCT_ACCEPTED`, does not enable
the paused native Codex Desktop schedules, and grants no authority to merge or publish.

## Git authority

- fetched `origin/master`: `3404b234e511add01d7b5a645ceaef3bf7765c36`;
- verified task-branch starting HEAD: `2909c66da8f28c64e33a4bae56b3ad76c4897a48`;
- branch: `codex/v1-final-product-four-window-closed-loop-completion-v1`;
- final commit and remote parity are recorded in the task handoff after the evidence commit is pushed.

## Corrected behavior

### Material-event priority

A material event still never creates a fifth automatic opportunity. It creates a restart-safe,
idempotent priority artifact bound to the existing durable work item. The next scheduled
opportunity receives the current headline IDs, source references, and update-chain identities.
Deterministic preselection adds a visible bounded score only to a matching candidate that already
passed eligibility; the priority cannot grant factual, numeric, evidence, or publication authority.
The item is terminalized through canonical state transitions after consumption, or after its
existing freshness horizon expires.

Controlled proof state:

`DISCOVERED -> EVIDENCE_PENDING -> EVIDENCE_BLOCKED -> REJECTED`

The terminal reason is `MATERIAL_EVENT_PRIORITY_CONSUMED`; the routine task count remains four.

### Derivative recovery

- `UNKNOWN_WRITE` always performs exact readback/reconciliation before any age decision and is
  never blindly retried.
- A current, still-useful `RECONCILED_ABSENT_SAFE_TO_RETRY` derivative can be attempted once; an
  immutable incident marker prevents another recovery attempt.
- A stale derivative becomes `DERIVATIVE_EXPIRED_STALE_NO_WRITE`; canonical Substack truth and
  reconciliation history remain intact.
- Recovery work is capped at nine exact rows per pass and reports `safely_attempted`,
  `stale_expired`, and `backlog_remaining`.
- A nonzero safe backlog blocks a new publication transaction instead of silently adding work.

The final production-shaped clone reported one bounded safe attempt, 20 historical stale rows
expired without a write, one exact readback, and one explicitly blocking backlog row.

### Real read-only performance capability

No permission or OAuth scope was widened. Each collection attempt is bound to the durable exact
public object and makes at most one provider request. Unavailable data is never zero-filled.

| Destination | Current collector capability | Interaction text | Exact bounded probe |
|---|---|---|---|
| Substack | first-party visible post stats through existing Edge JIT seam | not exposed | `COLLECTED`: total views 25, recipients 3, open rate 0.3333 |
| Telegram | not exposed by current bot binding | not exposed | `NOT_EXPOSED`, zero requests |
| X | not exposed by current authorized binding | not exposed | `NOT_EXPOSED`, zero requests |
| Discord | exact webhook-message reactions when current webhook binding and exact identity authorize it | not exposed | `UNAVAILABLE`: no exact confirmed/reconciled object, zero requests |
| LinkedIn | restricted member-social read permission required | permission required | `PERMISSION_REQUIRED`, zero requests |
| Facebook Page | likes/comments/shares when the current binding authorizes exact read | comments when authorized | `NOT_EXPOSED`: no exact confirmed object, zero requests |
| Instagram Business | likes/comments when the current binding authorizes exact read | comments when authorized | `NOT_EXPOSED`: no exact confirmed object, zero requests |
| Threads | replies when the current binding authorizes exact read | replies when authorized | `UNAVAILABLE`: no exact confirmed/reconciled object, zero requests |
| YouTube Community | not exposed by current authorized binding | not exposed | `NOT_EXPOSED`: no exact confirmed object, zero requests |

Probe evidence persists normalized metrics/status only; raw responses and secrets are absent.
Passive interaction projections retain hashes and bounded classifications only, remain untrusted,
and grant zero reply, reaction, DM, or comment-write authority.

### Learning truth

- Global sample count is distinct real article/work-item count.
- Each dispatch score first averages its observation windows; the article score then averages its
  destination dispatches. Nine destinations with four windows each therefore remain one global
  article vote.
- Global small-sample guard is five distinct articles; four holds and five passes that guard.
- Package learning remains destination-local, dedupes by work item within each destination, and
  requires three distinct article votes for a package preference.
- Generic social engagement cannot update SEO fields. SEO remains
  `HOLD_INSUFFICIENT_SEARCH_EVIDENCE` until real authorized search impressions plus clicks, CTR,
  or position reach the existing minimum support/confidence rules.

The controlled proof created 36 observations across nine destinations for one work item. It
produced one global article record and exactly one package record for each destination. SEO had
zero search-specific samples and remained HOLD.

### UI truth

The V5 design remains intact. Platforms now separates current collector capability, durable
observation-history presence, interaction capability, search capability, and next observation.
A historical observation cannot change a current unsupported or permission-required collector
into a supported one. Performance groups normal-viewport cards into Early, Intermediate, Daily,
and Long Tail sections, shows the capability matrix, and preserves unavailable/auth/permission
states without fake zeros.

Readable 1440 x 1000, 100%-zoom evidence:

- `A:\Capital Chronicle\Runtime\ContentOps\browser_qa\v1_four_window_bounded_correction_20260816\performance-01-top.png`
- `A:\Capital Chronicle\Runtime\ContentOps\browser_qa\v1_four_window_bounded_correction_20260816\performance-02-middle.png`
- `A:\Capital Chronicle\Runtime\ContentOps\browser_qa\v1_four_window_bounded_correction_20260816\performance-03-late.png`
- `A:\Capital Chronicle\Runtime\ContentOps\browser_qa\v1_four_window_bounded_correction_20260816\performance-04-bottom.png`
- `A:\Capital Chronicle\Runtime\ContentOps\browser_qa\v1_four_window_bounded_correction_20260816\platforms-01.png`
- `A:\Capital Chronicle\Runtime\ContentOps\browser_qa\v1_four_window_bounded_correction_20260816\learning-01.png`

## Production-shaped zero-write proof

The proof opens the real production SQLite authority read-only, uses SQLite backup to create a
temporary writable clone, builds the real production composition on the clone, and substitutes
controlled zero-write transports. It exercises recovery, freshness decisions, observation
scheduling/collection, passive interactions, learning, active-policy briefing, material-event
consumption, Desktop next-opportunity construction, and all nine publication-coordinator routes.

Evidence:

- structural proof:
  `A:\Capital Chronicle\Runtime\ContentOps\evidence\v1_four_window_bounded_correction_proof_v1.json`;
- bounded real read-only probe evidence:
  `A:\Capital Chronicle\Runtime\ContentOps\evidence\v1_four_window_bounded_correction_read_only_probes_v1.json`.

All 19 structural assertions pass. Capital Chronicle discovered 14 stores through
`duckdb_read_only`; story-scoped context reported `mutated_upstream=false`.

Production store hash before and after:

`d66758f97fbf6b985cb9be3d2df71da231e28221845dbbbea2af91ddcff4fd0e`

- production-store bytes changed: no;
- public adapters called: zero;
- public writes: zero;
- comment writes: zero;
- V2 runtime mutations: zero.

## Validation

- focused Python: `156 passed, 2 deselected`;
- the two deselected tests are pre-existing unrelated fixtures:
  `test_intake_delta_builds_stable_zero_llm_material_event` and
  `test_full_canonical_shadow_reaches_article_review_and_platform_package`;
- new/changed correction tests, publication recovery, Desktop briefing, LinkedIn, observer,
  learning, supervisor, and UI read-model coverage all pass;
- V5 focused Vitest: `11 passed`;
- V5 TypeScript/Vite production build: pass;
- production-shaped clone proof: pass;
- `git diff --check`: pass;
- generated deterministic context: `CODEGRAPH_CURRENT`.

Pytest emitted a Windows temp-directory cleanup permission warning after the passing run; it did
not change the test exit code or repository/runtime authority.

The broader legacy security-scan test remains red before reaching this correction's new observer:
its existing allowlist does not include the already-present `daily_app_launcher_v1.py` urllib
usage. This bounded task registers `destination_performance_observer_v1.py` in the read-only
network/config allowlists but intentionally does not reclassify the unrelated launcher.

## Exact next action

`OWNER_AUDITS_CORRECTION_THEN_FAST_FORWARDS_TO_MASTER_AND_RUNS_EXACTLY_ONE_MANUAL_GO_LIVE_CANARY_BEFORE_ENABLING_ANY_FOUR_WINDOW_SCHEDULE`
