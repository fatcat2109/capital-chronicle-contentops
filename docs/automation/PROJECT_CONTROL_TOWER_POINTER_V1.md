# Project Control Tower pointer V1

This document is a small cross-repository pointer. It is not a ContentOps status
ledger, capability registry, runtime receipt or publication instruction.

Canonical audit projection:

- Repository: `fatcat2109/Headline-Raw-data-json`
- Control Tower path: `docs/project_control_tower/`
- Snapshot ID observed by this pointer: `20260919T044605Z-f9dcbe752b39`
- Observation scope: Main App, ContentOps remote master, discovered local Git stores
  and registered worktrees, database Builder read-only inputs, and paginated PR metadata.
- Snapshot claim: `PARTIAL_AUDIT_COVERAGE_REVIEW_REQUIRED`

Read the canonical Control Tower `README.md` and generated master checklist before
using this pointer. The snapshot is a dated derived projection; it does not replace
this repository's current authority, North Star, owner state or runtime evidence.

This pointer intentionally contains no duplicate capability rows, source bodies,
runtime databases, browser/session material, credentials, account data or raw logs.
It does not authorize a scheduler, model/provider call, publication, deployment or
external write. ContentOps publication and readback continue to be owned by the
current ContentOps authority and `DurablePublicationCoordinator`.

The pointer is bound to the snapshot ID above. A later manual refresh must create a
new pointer change only when the operator wants ContentOps to reference the new
snapshot; silently changing the ID would make cross-repository evidence ambiguous.
