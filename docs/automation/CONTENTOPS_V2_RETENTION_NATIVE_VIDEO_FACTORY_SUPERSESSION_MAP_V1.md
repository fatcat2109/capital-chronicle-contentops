# Capital Chronicle ContentOps V2 — Retention-Native Video Factory Supersession Map V1

Authority date: 2026-08-12
Status: `SUPERSEDED_NON_ROUTING_BY_SUPERSESSION_MAP_V2_AND_2026_08_31_OWNER_PAUSE`

This map is historical. Its concurrent-V2 and exact-next claims are superseded. Main V2 is paused,
and the lightweight Speech Highlight Relay is a separate product under its own current pointer.

This map resolves conflicts between the current owner-approved retention-native V2 direction and older Tier-2 plans, rejected experiments, and historical pause/routing documents.

## 1. Current canonical authority

Highest current committed V2 planning authority after this update:

1. `docs/status/CONTENTOPS_V2_RETENTION_NATIVE_VIDEO_FACTORY_DIRECTION_OVERLAY_V1.md`
2. `docs/automation/CONTENTOPS_V2_RETENTION_NATIVE_VIDEO_FACTORY_NORTH_STAR_V1.md`
3. `docs/automation/CONTENTOPS_V2_RETENTION_NATIVE_VIDEO_FACTORY_MASTER_PLAN_V1.md`
4. `docs/automation/CONTENTOPS_V2_RETENTION_NATIVE_VIDEO_FACTORY_TASK_GRAPH_V1.md`
5. `docs/automation/CONTENTOPS_V2_RETENTION_NATIVE_VIDEO_FACTORY_CURRENT_EXECUTION_POINTER_V1.md`
6. this supersession map
7. exact task implementation/evidence after independent acceptance

Jim's newer explicit owner instruction still outranks committed product-direction documents if a later conflict occurs.

## 2. Prior Tier-2 Master Plan

File:

`docs/automation/CONTENTOPS_TIER2_PRO_VIDEO_FACTORY_MASTER_PLAN_V1.md`

Historical value:

- renderer-neutral `VideoProgram` doctrine;
- short-form native versus long-form separation;
- deterministic-first asset philosophy;
- rights/provenance requirements;
- programmable compositor direction;
- bounded revision;
- upload/readback/performance stages.

Superseded where conflicting:

- `APPROVED_FUTURE_EXECUTION_PLAN_POST_TIER1` / `NOT_CURRENT_NOT_STARTED` routing;
- mandatory wait for Tier-1 freeze;
- scene-centric creative model without explicit engagement/beat/edit/audio contracts;
- long-form 15–45 minutes as a required demonstration even when story depth does not support it;
- QA that does not explicitly include retention/static-run/beat-level diagnostics;
- product direction that does not yet include autonomous channel-growth/series/packaging/retention learning.

Current rule:

Preserve useful architecture history but follow the retention-native plan for all new V2 work.

## 3. Tier2-A

Task:

`TASK_CONTENTOPS_TIER2_A_LOCAL_LONG_FORM_AND_SHORT_FORM_PROGRAMMABLE_VERTICAL_SLICE_V1`

Classification:

`ENGINEERING_REFERENCE_NOT_CREATIVE_BASELINE`

Keep/selectively reuse:

- renderer-neutral program ideas;
- governed evidence bindings;
- local rendering/caching/package hashing;
- no-publication discipline.

Do not treat its visual/narrative output as accepted creative quality.

## 4. Historical Tier2-B Remotion branch

Branch:

`task/tier2-b-remotion-multimodal-bakeoff-v1`

Classification:

`REJECTED_VISUAL_PRODUCT_REFERENCE_ONLY`

Do not merge or continue its creative visual grammar. Inspect only for bounded engineering lessons when useful.

## 5. Rejected direct-image 9Router-contract branch

Branch/HEAD:

`task/tier2-image-generation-9router-contract-correction-v1` / `8b043a5`

Classification:

`REJECTED_PROVIDER_CONTRACT_IMPLEMENTATION`

Do not import its direct-host/generic-9Router coupling or add `ai.api-cheap.site` to the generic 9Router adapter.

The accepted direct-image capability is the dedicated isolated boundary already on master using `AI_API_CHEAP_API_KEY`.

## 6. Rejected V2 creative-system rebuild

Branch/HEAD:

`task/tier2-v2-creative-system-rebuild-v1` / `d231b54e026570442d9fd9269b61e55c3de31d21`

Classification:

`REJECTED_CREATIVE_PRODUCT_REFERENCE_ONLY`

Useful lessons/components that may be re-inspected and selectively reimplemented:

- semantic versus runtime identity;
- exact asset/narration cache dependencies;
- direct `gpt-5.5` illustration integration;
- rights-aware real-entity photo primitive;
- real FFmpeg transitions;
- selective scene/chapter rerender;
- deterministic media QA;
- package lock;
- multimodal critic scene/time coverage;
- no-filler decision when a story does not support long-form runtime.

Explicitly superseded creative assumptions:

- hardcoded Treasury program as V2 creative benchmark;
- one visual primitive/state per long narration scene;
- entrance animation followed by long holds;
- captions supplying most visible change;
- static editorial cards as the dominant grammar;
- narration-only output receiving product-quality acceptance;
- style-only revision whitelist;
- weak engagement/hook/payoff representation;
- contact-sheet-only motion acceptance.

Do not merge this branch into master. New creative implementation starts from fresh current master.

## 7. Historical video foundation/pause docs

Paths under:

`docs/automation/VIDEO_FOUNDATION_AND_PAUSE_V1/`

Classification:

`HISTORICAL_REFERENCE_ONLY_UNLESS_CURRENT_AUTHORITY_EXPLICITLY_CITES_A_BOUNDARY`

Their prior pause/lock sequencing does not block the current owner-approved concurrent V2 lane. Safety/provider findings remain useful historical evidence if still applicable.

## 8. V1/V2 concurrency

Older language that says Tier-2 must wait until Tier-1 final freeze is superseded by Jim's active owner reprioritization.

Current rule:

- V1 FDA-G/evidence-gate work continues independently;
- V2 may build concurrently;
- V2 must remain isolated from live V1 runtime/store/browser/publication authority;
- neither lane may reset or reinterpret the other's accepted durable state;
- V2 still has zero public-video-write authority until the explicit later tasks.

## 9. Duration doctrine supersession

Old interpretation:

`LONG_FORM_EDITORIAL_15_45M` can be read as requiring a 15–45 minute render to prove Tier-2.

Current interpretation:

15–45 minutes is a supported hero/long-form product range only when the selected story earns that depth. The factory must produce shorter mid-form or short-only output, or abstain, rather than add filler.

## 10. Engagement/retention doctrine supersession

Any earlier plan that treats a scene graph plus motion primitives as sufficient creative direction is superseded.

Current creative hierarchy:

```text
VideoOpportunity
→ EngagementBrief
→ VideoProgram
→ NarrativeBeatGraph
→ EditDecision/MotionBeatGraph
→ AssetPlan + AudioPlan
→ PlatformVariantPlan
→ renderer
→ RetentionDiagnostics + multimodal critic
```

## 11. Model-economics doctrine supersession

No older assumption that a frontier model should remain inside every production step is canonical.

Current rule:

- shared newsroom intelligence and evidence are amortized;
- deterministic/local mechanical work stays outside expensive semantic loops;
- routine bounded decisions use the cheapest model that meets measured quality;
- frontier-class reasoning is escalation for material value/defects;
- provider pricing/quota is benchmark-time data, not permanent architecture;
- Codex is the repo builder, not the runtime video factory.

## 12. Current next task

`TASK_CONTENTOPS_TIER2_V2_RETENTION_NATIVE_VIDEO_FACTORY_VERTICAL_SLICE_V1`

Required result:

`PASS_RETENTION_NATIVE_VERTICAL_SLICE_VISUAL_AUDIO_ACCEPTED`
