# ContentOps Final Architecture Truth Sweep — Master Plan Lock Input

Status: `NON_AUTHORITATIVE_MASTER_PLAN_LOCK_INPUT`

Baseline: `origin/master@f7c5543e08381f7f529e1b391a80a59f2032d76f`

This document answers the lock questions from exact current code, fetched refs, both local clones,
all registered worktrees, stashes/reflogs/unreachable commits, sanitized runtime/store truth,
supported native Automation observation, and historical proof. It does not change product
authority or authorize implementation, Automation mutation, browser/provider use, or public write.

## Executive truth

V1 is much closer to final than the current V3 plan says. The product already has its canonical
runtime/store/supervisor/publisher/UI, production-day accounting, eight-mode newsroom, governed
evidence path, Desktop-primary/SDK-fallback arbitration, one real reconciled nine-surface canary,
and a real four-window 4/32 attempt. The canary and accounting should not be rebuilt or repeated as
if unstarted.

The latest 4/32 attempt failed honestly at `0/4 / 0/32`, principally because 39 of 40 candidates
never crossed the evidence boundary; the one writer-boundary candidate exhausted its single
revision under the hard semantic contract. The later accepted evidence-foundation task then proved
four distinct evidence-ready candidates, exact URL discovery, route health, redirect repair, and
frozen worker transport—but at unacceptable discovery economics: 35 discovery calls and
10,237,897 discovery tokens. Therefore the only demonstrated net-new V1 product implementation gap
is quota-efficient batch/tail discovery on the existing evidence seam. Achieving 4/32 after that is
a current runtime/quality acceptance proof, not a request to recreate production-day accounting.

V2 is split across current master and a strong historical donor lineage. Current master already
contains the governed numeric boundary, rights/hash foundation, free-form chapter pipeline,
audio/caption/package substrate, zero-rerender localization base, and corrected six-surface shadow
publication control plane. A 48-commit branch-only lineage additionally proved an unattended V2
store/supervisor, Windows recovery, actual-media core proof, locale packages/timeline repair,
read-only V1 qualification, and the native HIGH -> fresh XHIGH -> HIGH relay. Those branch-only
capabilities should be reconciled and revalidated after V1, not rebuilt and not wholesale-merged
with stale authority.

## 1. Capabilities that already exist and must not be rebuilt

### V1

- Continuous zero-LLM intake, rolling sidecar universe, published memory, update-chain semantics,
  hierarchical assignment, preselection, and sourceability ordering.
- Eight editorial modes with claim/mode-proportional evidence and a narrow official-primary
  breaking fast lane.
- Deterministic public/official retrieval, locator recovery, redirect/publisher resolution,
  evidence/hash/freshness/claim binding, story-scoped reuse, delta-only reacquisition, and
  routing-only source health.
- Official Codex URL-only discovery with observed `webSearch` and deterministic post-retrieval
  authority checks. Preserve the capability; reject its old per-trigger production economics.
- Read-only Capital Chronicle/Core Analyzer context and exact story/use publication-authority
  boundary.
- Fresh Desktop HIGH -> isolated XHIGH writer -> deterministic validation, one same-worker repair,
  and Desktop-primary/SDK-fallback arbitration under one run identity.
- Source-marker/claim/numeric/utility/editorial/release-lock validation and the valid zero-canonical-
  media article path.
- Cross-midnight newsroom production-day ID, 4-floor accounting, bounded deficit/catch-up, and
  canonical persisted records.
- Canonical Substack, exactly eight derivative packages, nine transport registrations/JIT identity,
  destination-local continuation, readback/reconciliation, and no-blind-retry UNKNOWN_WRITE.
- One real owner-scoped nine-surface canary at `5327be9`, all nine reconciled, `UNKNOWN_WRITE=0`.
- Single durable schema-9 lineage/31-table physical store at the sweep epoch, launcher fencing,
  restart/recovery, performance observation, immutable learning-policy lineage, and V5 read model.

Exact rows and evidence are in `capability_matrix.json` entries `V1_01` through `V1_29`.

### V2 current master

- Tier2 governed input/numeric-authority boundary, rights/provenance/hash and factual-scene binding.
- Free-form chapterized Remotion substrate with dirty-range rendering, cache identity, picture
  assembly, separated audio rebuild, and mux.
- Kokoro technical baseline, actual-audio-timed captions, platform-neutral content-addressed
  packages, and zero-rerender language sidecars.
- Corrected official request-plan/readback state machine for YouTube video/Short, TikTok upload
  draft, Instagram explicit login variants, and Facebook Page Reels, with live writes disabled.

Exact rows: `V2_01` through `V2_06`.

## 2. What requires current runtime revalidation only

- 4/32 acceptance after the evidence-economics correction. The harness/accounting exists; the
  output result has not passed.
- Exact current host state before relying on it. At the sweep epoch four V1 Automations existed and
  were paused with correct model/effort/schedules, but their prompt hashes still differed from the
  configured intent.
- Calendar-time execution through those four tasks, cold start/restart, missed-primary SDK fallback,
  late-result suppression, cost bounds, and no duplicate article/public object.
- Fresh V5 desktop/mobile rendered screenshots at the current source/runtime epoch and owner visual
  acceptance.
- After V2 branch reconciliation: repeated fresh cross-domain story production and actual
  picture/sound/taste/publishability review. Historical media proof is not current final acceptance.

At the sweep epoch the production store opened read-only with `quick_check=ok`, nine destination
rows all identity-matched and ready, and `UNKNOWN_WRITE=0`. The Daily App itself was not running:
zero matching process rows and zero listener on 5174. This is a safe at-rest observation, not an
unattended-runtime PASS.

## 3. Genuine new implementation gaps

Only three survived search against current master, 149 fetched remote refs, both local clones,
114 registered worktrees, 10 stashes, reflogs/unreachable commits, and runtime evidence:

1. **V1 quota-efficient batch/tail discovery.** Reuse `OfficialCodexUrlDiscoveryProvider`, the
   same-candidate handshake, route health, deterministic retrieval/hash/freshness, and evidence
   gates. Change only invocation economics/batching/tail selection. No accepted branch already
   solves this.
2. **V2 selective single-stack reconciliation.** Current master lacks the proven branch-only
   `video/unattended_core_factory_v1`, `video/daily_operator_v1`, and
   `video/locale_activation_hardening_v1`. Port/reconcile useful current-compatible capability;
   never merge the stale branch authority wholesale and never keep two renderer/control stacks.
3. **V2 unified growth/learning loop.** Pieces exist in V1 performance/learning and the branch-only
   V2 read-only operator, but no accepted current-master flow closes qualified V1/story input ->
   V2 production -> observation -> bounded packaging/priority learning under one authority model.

The failed 4/32 outcome, Automation enablement, current visual/audio judgment, and final acceptance
are not mislabeled as new implementation merely because they lack current runtime proof.

## 4. Stale or false current-plan assumptions

- The real Italy public canary is not pending; it completed and reconciled all nine surfaces.
- The 4/32 campaign is not unstarted; its first real four-opportunity proof failed 0/4.
- Production-day accounting/catch-up is not missing; it is current master code with focused tests
  and an actual failed-day record.
- The four native V1 tasks are no longer host-unproven. They currently exist and are paused; what
  remains unproven is normalized prompts, calendar-time execution, and unattended behavior.
- First-party locator/publisher resolution is not the next gap; it merged before the accepted
  evidence foundation.
- `docs/CURRENT_CONTEXT.md`, the product-direction overlay, `V1_CONTEXT.md`, the V3 plan sequence,
  and the stale-doc manifest do not agree with the latest repository evidence.
- The V2 pointer contains a missing current-plan link and does not enumerate the accepted branch-only
  donor lineage.
- CodeGraph's older-looking `source_head` is not itself stale: digest-based `--check` is current.

See `authority_conflict_matrix.json` for exact files, claims, evidence, and lock actions.

## 5. Hidden/local/historical findings that change sequencing

- Current master contains three stronger V1 packets than the V3 plan acknowledges: the real live
  canary, the failed 4/32 attempt, and the later four-ready-candidate evidence foundation.
- The primary clone has 108 worktrees, 117 local branches, two stashes, 33 reflog-only commits, and
  43 unreachable commits. No commit is reachable only from its local branches and absent every
  fetched remote.
- A second full clone at `A:/Capital Chronicle/tools/cc-live-contentops` has six worktrees, eight
  stashes, 57 reflog-only commits, 243 unreachable commits, and two commits absent every remote.
  Those two local-only commits are fully product-subsumed by current master; no recovery is needed.
- The only material uncommitted product WIP is the V2 breaking-news crisp-master tree. It is
  incomplete and conflicts with current authority (separate ledger, deterministic aesthetic
  thresholds, retired MAX/ULTRA); preserve fingerprints only.
- Seventeen untracked Codex Desktop UIA/focus/input experiments exist in the canonical checkout.
  They are unsupported and must never become the execution bridge.
- The branch `origin/task/v2-native-staggered-automation-relay-shadow-correction-v1@10d18873`
  contains the accepted V2 donor sequence through durable factory, actual-media proof, Windows
  recovery, locales, read-only trigger, and native relay.

## 6. Roadmap items that can be deleted

- Build production-day accounting or bounded deficit recovery.
- Prove that four Automations exist merely by creating/configuring them; do not create a fifth.
- Publish the same Italy canary again.
- Rebuild canonical Substack/eight-derivative transports, UNKNOWN_WRITE recovery, durable store,
  Desktop/SDK arbitration, or V5 production-day mechanics.
- Rebuild the V2 official publication request-plan control plane.
- Rebuild the branch-proven V2 unattended store/supervisor, Windows recovery, locale timing, or
  native relay from zero.
- Run another HIGH/XHIGH/ULTRA fixed-renderer bakeoff, restore a fixed compositor, or revive the
  legacy Hormuz raster/oil asset universe as the professional default.
- Restore legacy V6 scheduler/live HTTP/persistent Antigravity control planes.

## 7. Remaining V1 acceptance blockers in dependency order

1. Owner audit/lock of this truth sweep and the four-ready-candidate foundation. This must reconcile
   the conflicting current authority docs before another builder routes from them.
2. Owner-authorized implementation of quota-efficient batch/tail discovery on the existing seam,
   with exact economics receipts and no weaker evidence gate.
3. Fresh zero-write evidence-foundation replay proving an economically credible ready pool under
   current source conditions.
4. Fresh 4/32 proof using the existing production-day harness and current HIGH/XHIGH path. If the
   same semantic failure repeats, repair only the exact demonstrated article transport/contract
   defect; do not lower factual/numeric/source or reader-value gates.
5. Owner-gated prompt normalization of only the four existing paused Automations, followed by fresh
   supported readback. No enablement is implied.
6. Explicit owner decision on enabling those four tasks and on any routine public-write expansion.
   Prompt update, task enablement, and public-write scope are separate gates.
7. Calendar-time unattended/cold-start/fallback/duplicate-suppression proof on current master. Start
   with zero public write unless the owner grant expressly includes a bounded live obligation.
8. Fresh current-source V5 runtime/browser screenshots and Jim/ChatGPT visual/truth audit.
9. Explicit `V1_FINAL_PRODUCT_ACCEPTED` owner decision and freeze. Do not infer it from the sweep.

The real nine-surface canary is completed technical evidence and is not a blocker to rebuild.

## 8. V2 work after V1

1. Lock the exact V2 donor commits/capabilities and selectively reconcile them onto fresh master,
   preserving current free-form authority and zero public write.
2. Revalidate the shared unattended store/supervisor, Windows path/duration repair, transcript/audio
   timing, locale packages, V1 read-only qualification, and native relay under current dependencies.
3. Produce fresh non-oil/non-Hormuz qualified cross-domain stories through the asset-first sequence.
4. Obtain actual-media XHIGH and Jim/ChatGPT review for a strong Short and justified mid/longform;
   treat visual/audio taste as evidence, not deterministic threshold.
5. Close repeated-job soak, cache/recovery, TCO, transcript/package truth, and integrated V1->V2
   growth/learning.
6. Only under a later exact owner grant, reverify destination identity/rights/provider contracts and
   conduct bounded video publication canaries. Current video public-write authority remains zero.

## 9. Owner-gated boundaries that remain

- Any authority-file rewrite or master-plan lock.
- The next V1 implementation task; the current sweep authorizes none.
- Native Automation prompt update, enablement, disablement, deletion, or creation.
- Any additional V1 public write or widening from a single canary to routine publication.
- Credentials/session/security expansion, browser account identity, provider OAuth, or live draft.
- Final V1 acceptance and protected release/freeze decision.
- Starting V2 work before V1 gate; accepting actual picture/sound/voice; rights/legal release.
- Any V2 draft/private/unlisted/public platform write; current authority is zero.
- Any Capital Chronicle/Core Analyzer publication/numeric authority expansion.

## 10. Shortest product-capability roadmap

```text
OWNER PLAN LOCK
  -> V1 quota-efficient batch/tail discovery on existing evidence seam
  -> fresh economical four-ready pool
  -> fresh 4/32 zero-write acceptance proof
  -> normalize/read back only four existing paused Automations
  -> owner enablement/public-write scope decision
  -> calendar-time unattended/cold-start/fallback proof
  -> current V5 visual/truth audit
  -> explicit V1 acceptance/freeze
  -> selective V2 donor reconciliation
  -> fresh cross-domain V2 actual-media/soak/growth loop
  -> owner-gated V2 publication expansion later
```

## Exact files/sections for the subsequent MASTER PLAN LOCK task

Change only after owner audit:

1. `AGENTS.md`
   - Section 3 anti-drift examples: retain, but align completed host/canary facts.
   - Section 6 host/Automation paragraph: keep paused truth and prompt mismatch; remove canary-ready
     sequencing that predates the real canary.
   - Section 11 current sequence: replace Italy audit/publication-next language with foundation ->
     quota-efficient discovery -> 4/32 rerun -> unattended/V5 sequence.
2. `docs/automation/CONTENTOPS_CURRENT_AUTHORITY_AND_SUPERSESSION_MAP_V1.md`
   - Keep the foundation economics override.
   - Add explicit accepted real canary and failed 4/32 facts so they cannot be scheduled again.
3. `docs/automation/CONTENTOPS_CURRENT_STALE_DOCS_MANIFEST_V1.md`
   - Add exact stale sections in `docs/CURRENT_CONTEXT.md`,
     `docs/status/CURRENT_PRODUCT_DIRECTION_OVERLAY.md`, and `docs/codegraph/V1_CONTEXT.md`.
4. `docs/automation/CONTENTOPS_FINAL_PRODUCT_NORTH_STAR_V3.md`
   - Section 11: mark live canary complete, 4/32 attempted/failed, foundation complete.
   - Section 12: preserve UI contract but remove implication that mechanics are not built.
5. `docs/automation/CONTENTOPS_FINAL_PRODUCT_MASTER_PLAN_V3.md`
   - Sections 2–3: replace canary-ready state with real canary + failed throughput + foundation.
   - Delete section 2.1 as an implementation gap; retain accounting as accepted foundation.
   - Sections 4–9 and 11: rebase sequence/blockers on quota-efficient discovery and current runtime
     proof; do not instruct another Italy write.
6. `docs/automation/CONTENTOPS_FINAL_DAILY_APP_V1_CURRENT_EXECUTION_POINTER_V3.md`
   - Preserve current next-capability statement; add the exact post-lock acceptance chain and clarify
     real canary/failed 4/32 are completed evidence, not routing tasks.
7. `docs/CURRENT_CONTEXT.md` and `docs/status/CURRENT_PRODUCT_DIRECTION_OVERLAY.md`
   - Replace host-unproven/accounting-gap/old next-task claims with the locked state.
8. `docs/codegraph/V1_CONTEXT.md`
   - Refresh curated current state, current blocker, evidence-foundation flow, and validation
     sequence. Remove locator-next and repeat-canary wording.
9. `docs/automation/CONTENTOPS_V2_RETENTION_NATIVE_VIDEO_FACTORY_CURRENT_EXECUTION_POINTER_V2.md`
   - Fix the missing `CONTENTOPS_V2_PRO_VIDEO_FACTORY_MASTER_PLAN_V1.md` link.
   - Add the exact branch-only donor manifest and current-master reconciliation boundary without
     authorizing V2 work.
10. Generated `docs/codegraph/INDEX.md`, `docs/codegraph/V2_CONTEXT.md`, and
    `docs/codegraph/graph.json`
    - Regenerate only after the authoritative files above change.
    - Consider a compact owner-designated branch-reference manifest; do not index runtime payloads
      or all historical noise.

Do not rewrite the demoted nine-surface handoff as if its old run said something else. Keep it
historical and remove it from active routing.
