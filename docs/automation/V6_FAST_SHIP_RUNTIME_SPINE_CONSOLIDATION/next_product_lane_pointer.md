# Next Product Lane Pointer

Recommended next heavy task:

`TASK_CONTENTOPS_V6_DISCORD_DRY_RUN_OUTBOX_AND_OPERATOR_APPROVAL_SPINE_HEAVY_BATCH_V0`

## Scope

One heavy product milestone should cover:

- Discord payload model.
- Outbox dry-run packet.
- Preview/hash generation.
- Operator approval record.
- Redacted audit packet.
- Manual fallback path.

The task may prepare live pilot readiness, but must not send live unless that task explicitly scopes a live write with operator approval and safety budgets.
