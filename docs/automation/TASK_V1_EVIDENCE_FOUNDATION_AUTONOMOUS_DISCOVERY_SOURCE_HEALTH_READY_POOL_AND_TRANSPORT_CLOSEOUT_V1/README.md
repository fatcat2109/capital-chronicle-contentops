# V1 Evidence Foundation Closeout V1

Authority date: 2026-08-23
Classification: `PASS_V1_EVIDENCE_FOUNDATION_4_ARTICLE_READY_ZERO_WRITER`

## Boundary

This task stops at the governed pre-writer evidence boundary. It generated no article or derivative,
invoked no XHIGH/editorial worker, performed no public write, mutated no Capital Chronicle authority,
used no browser/CDP transport, and did not enable or create an Automation.

## Canonical autonomous discovery path

`canonical newsroom cycle -> tracked evidence acquisition -> deterministic acquisition -> exact
SOURCE_DISCOVERY_REQUIRED -> ChatGPT-authenticated official Codex App Server Sol/HIGH URL-only role
-> strict local discovery-contract/action validation -> same candidate and headline IDs ->
deterministic registered loader -> live retrieval -> canonical content hash -> freshness and claim
gates -> governed evidence-ready boundary`

The discovery turn persisted URLs and sanitized action/usage receipts only. Search snippets, model
summaries, and model assertions have zero evidence authority. The local action validator requires an
observed `webSearch` item and rejects every result item outside `userMessage`, `reasoning`,
`webSearch`, and `agentMessage`.

## Current production-day proof

The frozen current universe contained 377 headlines. Four bounded opportunities used 24, 23, 24,
and 20 deterministic network requests (91 total, below the unchanged 96 ceiling). Four distinct
candidate identities reached evidence `PASS`, freshness `PASS`, capability coverage, claim-contract
`PASS`, at least one source-bound supported claim, and no unresolved blocker:

1. U.S.-Canada trade negotiation/tariff development — cluster
   `rolling-x-global-cluster-f50b349b129dc211b361`; 1 supported claim; evidence SHA-256
   `7b62ac3529f20c8def1f9069432ced8b140d79ed8975bf153a3e449a5dd2901a`.
2. Iranian permission for Iraqi tankers through Hormuz — cluster
   `rolling-x-global-cluster-9ba9db84f5a3c805eeee`; 6 supported claims; evidence SHA-256
   `271d14c211866d9622131142cc76b295c7aaf4f81e0b4f32f70d57b79ddeba99`.
3. Central Nigeria killings and abductions — cluster
   `rolling-x-semantic-reserve-9a02197a8c4f60357127`; 2 supported claims; evidence SHA-256
   `2d86eb5f80c620da5e669097410540e566b6aaa0f6c8e509a09c63c620c52656`.
4. Fatal police-vehicle crash near Middlesbrough — cluster
   `rolling-x-semantic-reserve-16d00a53d70d5c905de5`; 2 supported claims; evidence SHA-256
   `f474a1ee4dc6f1cc47dcdee21948ad134a403a800d2682c20413204906cd895f`.

The separate Canadian prime-minister trade statement cluster
`rolling-x-global-cluster-9b00d4b61f643fe7fcda` passed its own evidence gate but was
conservatively excluded from accepted yield as a semantic update chain of candidate 1.

Accepted-evidence yield was `4 / 91 = 0.043956` per deterministic request. Grounded synthesis used
31 calls and 251,703 tokens versus the 55b baseline's 17 calls, 73,737 tokens, 110 requests, and zero
ready candidates. URL-only discovery used 35 calls and 10,237,897 tokens. No cost receipt exists and
no cost-saving claim is made.

The exact candidate records, source URLs/hashes, autonomous-discovery receipts, all 31 abstention
rows with exact blockers, source-health carry-forward proof, and economics are in
`current_production_day_evidence_foundation_proof_v1.json`. Receipt SHA-256:
`cc1ce48e034259a5e0b953eac54d62d277e524f6fefb3fd2cb3f2452224ff329`.

## Source health and redirect scope

The supervisor persists the routing-only snapshot atomically as `source_route_health_v1.json` under
the existing output root and injects it into subsequent opportunities. Production receipts showed
185 shared exact routes between adjacent opportunities, three routes with monotonically advanced
observations, and actual recent exact-route suppression. A failed exact route does not suppress a
different same-publisher route. Health grants no factual, numeric, permission, or publication
authority.

The only new cross-host publisher relationship is the exact registered CNBC short-host mapping
`cnb.cx -> cnbc.com`. Unsafe unrelated cross-host redirects remain rejected; Google News remains a
locator only; and 401/403/404/WAF/paywall/dead-link failures are not relabeled as redirect fixes.
See `redirect_before_after_matrix_v1.json`.

## Frozen worker transport

The frozen Desktop artifact's top-level `editorial_output` envelope is losslessly unwrapped and the
existing aliases `canonical_editorial_headline -> title` and
`article_body -> substack_body_markdown` are applied without inventing a semantic field. This
mechanical mismatch consumes no editorial revision. The unchanged canonical validators then reject
the frozen article's remaining semantic defects with `GroundedArticleBuilderError`, proving the
hard article boundary still fails closed. No worker was called. See
`frozen_worker_transport_proof_v1.json`.

## Supporting artifacts

- `capability_archaeology_v1.json`
- `redirect_before_after_matrix_v1.json`
- `current_production_day_evidence_foundation_proof_v1.json`
- `frozen_worker_transport_proof_v1.json`
- `tests_and_validation_v1.json`

This PASS authorizes owner audit only. It does not authorize 4/32 article generation, an article,
an XHIGH worker, Automation mutation, or public write.
