# ContentOps V2 — HIGH Parent / XHIGH Video-Creative Reasoning Owner Correction V1

Authority date: 2026-08-17
Status: `CURRENT_OWNER_CORRECTION / CONTROLLING_REASONING_EFFORT_TOPOLOGY`
Owner: Jim

This correction supersedes every current V2 phrase that says or implies that the whole Codex Desktop App task/session runs at Extra High / XHIGH.

## Canonical reasoning-effort topology

`V2_DESKTOP_PARENT_REASONING_EFFORT = HIGH`

The normal Codex Desktop App parent/session runs `GPT-5.6 Sol / HIGH`.

HIGH owns ordinary V2 session work, including:

- waiting for or receiving V2 content ideas/opportunities;
- intake and qualification coordination;
- repository/task orchestration;
- Git/evidence reading;
- deterministic diagnostics and mechanical repair planning;
- invoking local deterministic render/audio/package/test tooling;
- waiting/polling and stage coordination;
- durable-state/checkpoint coordination;
- non-creative evidence/reporting work.

`V2_VIDEO_CREATIVE_REASONING_EFFORT = XHIGH`

Extra High / XHIGH is a bounded reasoning effort invoked by the HIGH parent/session only for high-value work directly related to creating, editing, or critically reviewing a Capital Chronicle V2 video, for example:

- institutional analytical decomposition for the video;
- narrative architecture and hook/re-hook strategy;
- narration/spoken-language authorship;
- story-specific visual composition and Remotion source;
- typography, motion, pacing, transitions, and sound-edit intent;
- actual-media creative review;
- bounded same-video creative revision.

XHIGH is not the default effort of the waiting/parent session. Do not keep an idle XHIGH task/session alive. Do not spend XHIGH on Git, rendering, transcoding, tests, path repair, waiting, polling, evidence formatting, commit, push, or other mechanical work.

The HIGH parent may invoke bounded XHIGH video-creative work when a qualified video job reaches a creative stage, then resumes HIGH coordination after explicit creative artifacts return. The exact supported Codex Desktop mechanism may vary, but this does not authorize CLI, `codex exec`, SDK/API/headless creative execution, 9Router creative execution, or provider-model substitution.

## Current runtime discrepancy

The current implementation at `de0d9748b36334d408f7cc845b8edda109df0ff8` records the Desktop session provenance itself as `xhigh`. That contract reflects the superseded interpretation and must be corrected before the next fresh actual-media proof.

The next task must distinguish at least:

- parent/session provenance: `GPT-5.6 Sol / HIGH`;
- bounded video-creative provenance: `GPT-5.6 Sol / XHIGH` when invoked for actual creative/video work;
- zero CLI/SDK/API/headless/9Router/provider creative fallback.

Do not erase the prior proof. Its failure evidence remains historically valid for the browser/process blocker, but its all-session-XHIGH provenance is not the current owner model.

## Current next task launch rule

Open the next fresh Codex Desktop App task/session at:

`GPT-5.6 Sol / HIGH`

The parent/session may invoke XHIGH only when actual video-creative work is reached. The bounded Windows path/browser diagnostic and mechanical repair itself stays HIGH.

This correction also supersedes conflicting wording in root `AGENTS.md`, generated V2 context, North Star/Master Plan bodies, owner override, execution pointer, handoff, or prior prompts until those generated/current files are regenerated or reconciled.
